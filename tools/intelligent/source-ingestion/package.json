{
  "name": "source-ingestion",
  "version": "1.0.0",
  "description": "Canonical, read-only source ingestion boundary",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests",
    "check": "npm run typecheck && npm run lint && npm test && npm run build"
  },
  "dependencies": {
    "workbook-inspector": "file:../../deterministic/excel/workbook-inspector",
    "cell-layout-extractor": "file:../../deterministic/excel/cell-layout-extractor",
    "excel-context-builder": "file:../excel/context-builder"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  }
}
