{
  "name": "excel-analyzer",
  "version": "1.0.0",
  "description": "AI-powered semantic analyzer for Excel context packages → Semantic IR",
  "type": "module",
  "main": "dist/analyzer.js",
  "types": "dist/analyzer.d.ts",
  "bin": {
    "excel-analyzer": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^22.0.0"
  }
}
